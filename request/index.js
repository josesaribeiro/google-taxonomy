const axios = require('axios');
const requiredArgs = ['r', 'e'];

function run({ r, e }) {
  console.warn('TODO: Implement request strategy');
  console.warn({ r, e });
}

module.exports = { run, requiredArgs };

